# 🚀 دليل سريع: أسئلة الفهم والاستيعاب

## ❓ ما هي المشكلة؟

عند استخراج أسئلة مثل:
- "أجب من الفقرة أعلاه..."
- "استخرج من القطعة التالية..."
- "اقرأ النص ثم أجب..."

**المشكلة:** السؤال يُستخرج بدون الفقرة، ويصبح غير مفهوم! ❌

---

## ✅ الحل

النظام الآن يتعرف تلقائيًا على هذه الأسئلة:
1. يستخرج الفقرة أو القطعة النصية كاملة
2. يحفظ الفقرة في حقل `option1`
3. يحدد نوع السؤال كـ `Comprehension`
4. يستخرج الإجابة من الفقرة

---

## 📊 مثال عملي

### ما يكون في الكتاب:

```
اقرأ الفقرة التالية ثم أجب:

"الماء ضروري للحياة. يغطي الماء 71% من سطح الأرض. 
يحتاج الإنسان للشرب من 6-8 أكواب يومياً."

السؤال: ما هي النسبة التي يغطيها الماء من سطح الأرض؟
```

### ما يُستخرج في قاعدة البيانات:

```json
{
  "question": "ما هي النسبة التي يغطيها الماء من سطح الأرض؟",
  "question_type": "Comprehension",
  "option1": "الماء ضروري للحياة. يغطي الماء 71% من سطح الأرض. يحتاج الإنسان للشرب من 6-8 أكواب يومياً.",
  "correct_answer": "71%",
  "lesson_title": "الماء"
}
```

---

## 🔧 كيف تستخدمه؟

### 1. استخراج (تلقائي)

```bash
# فقط ارفع الـ PDF كالعادة
curl -X POST "http://localhost:8000/extract" \
  -F "file=@textbook.pdf" \
  -F "subject_name=اللغة العربية" \
  -F "uploaded_by=teacher1"
```

النظام سيتعرف تلقائيًا على أسئلة الفهم! ✨

---

### 2. الحصول على الأسئلة

```python
import requests

# احصل على أسئلة الفهم والاستيعاب
response = requests.get(
    "http://localhost:8000/questions",
    params={"question_type": "Comprehension"}
)

questions = response.json()

for q in questions:
    print(f"السؤال: {q['question']}")
    print(f"الفقرة: {q['option1']}")
    print(f"الإجابة: {q['correct_answer']}\n")
```

---

### 3. عرض في واجهة المستخدم

```python
def display_question(question):
    if question['question_type'] == 'Comprehension':
        # عرض الفقرة أولاً
        print("📖 اقرأ الفقرة:")
        print(question['option1'])
        print("\n" + "="*50 + "\n")
    
    # عرض السؤال
    print(f"❓ {question['question']}")
    
    # عرض الإجابة إن وُجدت
    if question.get('correct_answer'):
        print(f"\n✅ الإجابة: {question['correct_answer']}")
```

---

## 🎯 أنواع الأسئلة المدعومة

| النوع | option1 |
|-------|---------|
| **Multiple Choice** | الخيار الأول |
| **Comprehension** | الفقرة النصية ⭐ |
| **Descriptive** | فارغ |
| **Short Answer** | فارغ |
| **True/False** | فارغ |

---

## 📝 أمثلة أخرى

### مثال علمي:

```
من القطعة أعلاه، استخرج مكونات البناء الضوئي.

الفقرة: "البناء الضوئي يحتاج إلى ضوء الشمس، ماء، وثاني أكسيد الكربون (CO₂)."
```

**يُستخرج:**
- `question`: "استخرج مكونات البناء الضوئي"
- `question_type`: "Comprehension"
- `option1`: "البناء الضوئي يحتاج إلى..."
- `correct_answer`: "ضوء الشمس، ماء، وثاني أكسيد الكربون"

---

### مثال أدبي:

```
اقرأ الأبيات ثم أجب: ما الفكرة الرئيسية؟

الأبيات: [أبيات الشعر...]
```

**يُستخرج:**
- `question`: "ما الفكرة الرئيسية؟"
- `question_type`: "Comprehension"
- `option1`: [الأبيات كاملة]

---

## ✨ المزايا

- ✅ **تلقائي**: لا حاجة لتدخل يدوي
- ✅ **ذكي**: يتعرف على أسئلة الفهم تلقائياً
- ✅ **شامل**: يستخرج الفقرة كاملة
- ✅ **دقيق**: يحافظ على جميع الرموز
- ✅ **متعدد اللغات**: عربي وإنجليزي

---

## 🔍 البحث والاستعلام

```python
# كل أسئلة الفهم
all_comp = requests.get(
    "http://localhost:8000/questions",
    params={"question_type": "Comprehension"}
).json()

# أسئلة فهم في مادة معينة
arabic_comp = requests.get(
    "http://localhost:8000/questions",
    params={
        "subject_name": "اللغة العربية",
        "question_type": "Comprehension"
    }
).json()

# أسئلة فهم سهلة
easy_comp = requests.get(
    "http://localhost:8000/questions",
    params={
        "question_type": "Comprehension",
        "question_difficulty": "Easy"
    }
).json()
```

---

## 📚 للمزيد من التفاصيل

راجع الملف الكامل: **COMPREHENSION_QUESTIONS_FEATURE.md**

---

**الحالة**: ✅ جاهز للاستخدام الآن!

